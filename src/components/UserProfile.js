import React from 'react'
import classes from './UserProfile.module.css'

const UserProfile = () => {
  return (
    <div style={{fontWeight: 'bold'}} className={classes.profile}>My User Profile</div>
  )
}

export default UserProfile